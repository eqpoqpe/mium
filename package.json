{
  "name": "mium",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "wasm:tryrs": "wasm-pack build libs/tryrs/ --target nodejs --scope mium --out-name index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "workspaces": [
    "apps/**/*",
    "libs/**/*"
  ],
  "devDependencies": {
    "mium": "^1.0.0"
  }
}
